# `labels/`

Reserved for a future genuine Silvaplana-lake-labeled historical dataset,
once `logs/kitesailing_observations.jsonl` accumulates enough multi-month
depth to backtest against directly (see CLAUDE.md's "Conventions" section
on the Samedan-vs-kitesailing labeling mismatch). As of this session that
log has zero accumulated history (checked: it has never been committed to
this repository - see `docs/STATION_RESEARCH.md`), so there is nothing to
put here yet.
